- Built new yt-dlp hook `ytb-checker` reporting latest uploads + dates on command `/ytb-checker` using channels list and yt-dlp metadata. Enabled hook and tested. Added channels list with the four requested channels.
- Built new hook `ytb-library` to monitor same channels, download transcripts for new video uploads, save raw VTT + info into `knowledge/ytb-library/<videoId>/`, store state, and expose `/ytb-library` command. Hook enabled and tested (needs remediation for yt-dlp 429 due to no JS runtime). Added placeholder skills/youtube-skill to keep doc dependencies satisfied.
- Updated cron flow: rewrote `scripts/youtube_channel_cron.py` to track per-channel state in `knowledge/channel-state.json`, fetch only latest video via yt-dlp `--playlist-items 1`, ingest new videos via youtube_to_knowledge, store transcripts, and log in `knowledge/processed-videos.json`. Added timestamp timezone fix in cron runner.
- Cron run 2026-03-08T11:29Z (run_youtube_channel_cron) now sees new videos and processed four latest resulting transcripts saved under knowledge/youtube.
