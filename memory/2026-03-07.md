
## Follow-ups
- Schedule the cron job 6x/day; log success/failures in `knowledge/processed-videos.json` and note accession in memory each run.
- Scheduled the YouTube channel scan cron job (`run_youtube_channel_cron.py`) to run every 4 hours; it appends a summary line to `memory/youtube_cron_runs.md` after each attempt.
